

import {NgModule} from '@angular/core';
import {Routes, RouterModule} from '@angular/router';

const ofertRoutes: Routes = [
];

@NgModule({
  imports: [RouterModule.forChild(ofertRoutes)],
  exports: [RouterModule]
})

export class OfertsRoutingModule {
}
